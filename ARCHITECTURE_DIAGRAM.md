# Vibe Search - Architecture & Data Flow Diagrams

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                         │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         Next.js Frontend (Port 3000)                  │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────┐ │   │
│  │  │   Search     │  │   Results    │  │  Explore  │ │   │
│  │  │  Interface   │  │    Grid      │  │   Feed    │ │   │
│  │  └──────────────┘  └──────────────┘  └──────────┘ │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────┘
                             │ HTTP/REST API
                             │ (JSON, FormData)
┌────────────────────────────▼─────────────────────────────────┐
│                    BACKEND API LAYER                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │         FastAPI Backend (Port 8000)                   │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────┐   │   │
│  │  │   Search    │  │    Feed      │  │  Health  │   │   │
│  │  │  Router     │  │   Router     │  │  Check   │   │   │
│  │  └──────┬──────┘  └──────┬──────┘  └──────────┘   │   │
│  │         │                 │                          │   │
│  │  ┌──────▼─────────────────▼──────┐                  │   │
│  │  │      Search Utilities          │                  │   │
│  │  │  - Vector Search               │                  │   │
│  │  │  - Filter Building             │                  │   │
│  │  └──────┬────────────────────────┘                  │   │
│  └─────────┼────────────────────────────────────────────┘   │
│            │                                                   │
│  ┌─────────▼─────────┐  ┌──────────────────┐                │
│  │   AI Models       │  │  Database         │                │
│  │  - CLIP (Images)  │  │  Connection       │                │
│  │  - Sentence Trans │  │  Management       │                │
│  └───────────────────┘  └──────────────────┘                │
└───────────────────────────────┬───────────────────────────────┘
                                │ SQL Queries
                                │ (Vector Search)
┌───────────────────────────────▼───────────────────────────────┐
│                      DATA LAYER                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │     PostgreSQL 18 + pgvector (Port 5432)            │   │
│  │  ┌──────────────┐           ┌──────────────────┐    │   │
│  │  │   products   │           │ scraped_images   │    │   │
│  │  │  - 491 rows  │           │  - 5+ rows       │    │   │
│  │  │  - Vectors   │           │  - Vectors       │    │   │
│  │  │  - Metadata  │           │  - Captions      │    │   │
│  │  └──────────────┘           └──────────────────┘    │   │
│  └──────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────┘
```

## 🔄 Complete Data Flow

### Text Search Flow:

```
┌─────────┐
│  User   │ Types "black sneakers"
└────┬────┘
     │
     ▼
┌─────────────────┐
│  Frontend       │ SearchInterface.tsx
│  - Captures     │
│  - Validates    │
└────┬────────────┘
     │ POST /api/search/text
     │ { query: "black sneakers", limit: 20 }
     ▼
┌─────────────────┐
│  Backend API    │ search.py
│  - Receives     │
│  - Validates    │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│  AI Model       │ get_text_model()
│  Sentence       │
│  Transformer    │
└────┬────────────┘
     │ Generate embedding
     │ [0.1, 0.2, ..., 0.9] (384 numbers)
     ▼
┌─────────────────┐
│  Search Utils   │ execute_vector_search()
│  - Build query  │
│  - Add filters  │
└────┬────────────┘
     │ SQL with vector search
     ▼
┌─────────────────┐
│  PostgreSQL     │
│  - Vector       │
│  similarity     │
│  search         │
└────┬────────────┘
     │ Results with similarity scores
     ▼
┌─────────────────┐
│  Backend API    │
│  - Format       │
│  - Add metadata │
└────┬────────────┘
     │ JSON response
     ▼
┌─────────────────┐
│  Frontend       │ ResultsGrid.tsx
│  - Display      │
│  - Show scores  │
└─────────────────┘
```

### Image Search Flow:

```
┌─────────┐
│  User   │ Uploads image file
└────┬────┘
     │
     ▼
┌─────────────────┐
│  Frontend       │ SearchInterface.tsx
│  - File input   │
│  - Preview      │
└────┬────────────┘
     │ POST /api/search/image/upload
     │ FormData: { image_file: File }
     ▼
┌─────────────────┐
│  Backend API    │ search.py
│  - Receives     │
│  - Validates    │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│  Image          │
│  Processing     │
│  - Load image   │
│  - Convert RGB  │
└────┬────────────┘
     │
     ▼
┌─────────────────┐
│  AI Model       │ get_clip_model()
│  CLIP           │
│  (OpenAI)       │
└────┬────────────┘
     │ Generate embedding
     │ [0.1, 0.2, ..., 0.9] (512 numbers)
     ▼
┌─────────────────┐
│  Search Utils   │ execute_vector_search()
│  - Vector       │
│  similarity     │
└────┬────────────┘
     │ SQL: ORDER BY embedding <=> query_vector
     ▼
┌─────────────────┐
│  PostgreSQL     │
│  - HNSW index   │
│  - Fast search  │
└────┬────────────┘
     │ Top 20 similar products
     ▼
┌─────────────────┐
│  Frontend       │ ResultsGrid.tsx
│  - Display      │
│  - Match scores │
└─────────────────┘
```

## 📊 Database Schema Relationships

```
┌─────────────────────────────────┐
│         products                  │
├─────────────────────────────────┤
│ id (PK)                          │
│ product_id (UNIQUE)              │
│ title                            │
│ category                         │
│ brand_name                       │
│ featured_image                   │
│ lowest_price                     │
│ extracted_colors[]               │
│ extracted_styles[]               │
│ extracted_brands[]               │
│ image_embedding vector(512)      │ ← CLIP embeddings
│ text_embedding vector(384)        │ ← Text embeddings
│ ...                              │
└─────────────────────────────────┘
         │
         │ (used for search)
         │
         ▼
┌─────────────────────────────────┐
│      scraped_images             │
├─────────────────────────────────┤
│ id (PK)                         │
│ source                          │
│ image_url                       │
│ caption                         │
│ hashtags[]                      │
│ engagement_count                │
│ image_embedding vector(512)     │ ← CLIP embeddings
│ ...                             │
└─────────────────────────────────┘
```

## 🔍 Vector Search Process

```
Query Image/Text
     │
     ▼
Generate Embedding
     │
     │ [0.1, 0.2, 0.3, ..., 0.9]
     ▼
┌────────────────────────────┐
│  PostgreSQL pgvector      │
│                            │
│  SELECT                    │
│    *,                      │
│    1 - (embedding <=>      │
│         query_vector)      │
│    as similarity           │
│  FROM products             │
│  WHERE embedding IS NOT    │
│    NULL                    │
│  ORDER BY embedding <=>    │
│    query_vector            │
│  LIMIT 20                  │
│                            │
│  <=> = Cosine Distance     │
│  HNSW Index = Fast Search  │
└────────────────────────────┘
     │
     │ Results sorted by similarity
     ▼
Products with scores
[0.95, 0.92, 0.88, ...]
```

## 🎯 Component Interaction

```
┌──────────────┐
│   User       │
└──────┬───────┘
       │
       │ 1. Types query / Uploads image
       ▼
┌─────────────────────┐
│  SearchInterface    │
│  (Frontend)         │
└──────┬──────────────┘
       │
       │ 2. HTTP Request
       ▼
┌─────────────────────┐
│  search.py          │
│  (Backend Router)   │
└──────┬──────────────┘
       │
       │ 3. Generate embedding
       ▼
┌─────────────────────┐
│  AI Model           │
│  (CLIP/Transformer) │
└──────┬──────────────┘
       │
       │ 4. Vector
       ▼
┌─────────────────────┐
│  search_utils.py    │
│  (Search Logic)     │
└──────┬──────────────┘
       │
       │ 5. SQL Query
       ▼
┌─────────────────────┐
│  PostgreSQL         │
│  (Vector Search)    │
└──────┬──────────────┘
       │
       │ 6. Results
       ▼
┌─────────────────────┐
│  ResultsGrid        │
│  (Frontend)         │
└─────────────────────┘
```

## 📦 File Organization

```
CultureCircleTask/
│
├── 📁 api/                    # Backend API
│   ├── main.py               # FastAPI app
│   ├── models.py             # Data models
│   ├── database.py           # DB connection
│   ├── search_utils.py       # Search logic
│   └── routers/              # API endpoints
│       ├── search.py         # Search routes
│       ├── health.py         # Health check
│       └── feed.py           # Scraped images
│
├── 📁 database/               # Database scripts
│   ├── schema.sql            # Table definitions
│   ├── import_products.py    # CSV import
│   └── setup_database.py     # Schema setup
│
├── 📁 embeddings/            # AI/ML
│   ├── generate_clip_embeddings.py    # Image AI
│   ├── generate_text_embeddings.py    # Text AI
│   └── check_embedding_status.py     # Status
│
├── 📁 metadata/              # NLP extraction
│   └── extract_metadata.py   # Colors, styles, brands
│
├── 📁 scrapers/              # Web scraping
│   ├── base_scraper.py       # Base class
│   ├── pinterest_scraper.py  # Pinterest
│   ├── instagram_scraper.py  # Instagram
│   └── run_scrapers.py       # Main script
│
├── 📁 frontend/               # Next.js app
│   ├── app/                  # Pages
│   │   ├── page.tsx          # Main page
│   │   └── layout.tsx        # Layout
│   ├── components/           # React components
│   │   ├── SearchInterface.tsx
│   │   ├── ResultsGrid.tsx
│   │   ├── ExploreFeed.tsx
│   │   └── ProductModal.tsx
│   └── types/                # TypeScript types
│
├── 📁 tests/                  # Test suites
│   ├── test_database.py      # DB tests
│   └── test_api.py           # API tests
│
└── 📄 Configuration files
    ├── requirements.txt       # Python deps
    ├── run_server.py         # Start backend
    └── start_all.ps1         # Start everything
```

## 🔑 Key Concepts Visualized

### Vector Embedding Concept:

```
Product Image                    Text Query
     │                              │
     │                              │
     ▼                              ▼
┌─────────┐                    ┌─────────┐
│  CLIP   │                    │Sentence │
│  Model  │                    │Transformer│
└────┬────┘                    └────┬────┘
     │                              │
     │ [0.1, 0.2, ..., 0.9]        │ [0.2, 0.3, ..., 0.8]
     │ (512 numbers)               │ (384 numbers)
     │                              │
     └──────────┬───────────────────┘
                │
                ▼
        Stored in Database
        as vector(512) or vector(384)
                │
                │ Similarity Search
                ▼
        Find products with
        similar vectors
```

### Search Process:

```
┌─────────────────────────────────────────┐
│           USER QUERY                     │
│  (Text or Image)                        │
└──────────────┬──────────────────────────┘
                │
                ▼
        ┌───────────────┐
        │  Generate    │
        │  Embedding   │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │  Vector       │
        │  Similarity   │
        │  Search       │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │  Apply        │
        │  Filters      │
        │  (Optional)    │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │  Rank by      │
        │  Similarity   │
        └───────┬───────┘
                │
                ▼
        ┌───────────────┐
        │  Return Top   │
        │  Results      │
        └───────────────┘
```

## 🎓 Learning Path Visualization

```
START HERE
    │
    ▼
┌─────────────────┐
│  README.md      │  Understand what it does
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  schema.sql     │  Understand data structure
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  import_products│  See how data is loaded
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  generate_*     │  Understand AI embeddings
│  _embeddings.py │
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  api/main.py    │  See API structure
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  search.py      │  Understand search logic
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  search_utils.py│  See vector search
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│  frontend/      │  Understand UI
└────────┬────────┘
         │
         ▼
    MASTERED!
```

Use these diagrams to visualize and explain the architecture! 🎨

